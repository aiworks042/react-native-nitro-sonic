///
/// JSoundOptions.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "SoundOptions.hpp"

#include "AudioCategory.hpp"
#include "JAudioCategory.hpp"
#include <optional>

namespace margelo::nitro::nitrosonic {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "SoundOptions" and the Kotlin data class "SoundOptions".
   */
  struct JSoundOptions final: public jni::JavaClass<JSoundOptions> {
  public:
    static constexpr auto kJavaDescriptor = "Lcom/margelo/nitro/nitrosonic/SoundOptions;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct SoundOptions by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    SoundOptions toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldVolume = clazz->getField<jni::JDouble>("volume");
      jni::local_ref<jni::JDouble> volume = this->getFieldValue(fieldVolume);
      static const auto fieldLoop = clazz->getField<jni::JBoolean>("loop");
      jni::local_ref<jni::JBoolean> loop = this->getFieldValue(fieldLoop);
      static const auto fieldSpeed = clazz->getField<jni::JDouble>("speed");
      jni::local_ref<jni::JDouble> speed = this->getFieldValue(fieldSpeed);
      static const auto fieldCategory = clazz->getField<JAudioCategory>("category");
      jni::local_ref<JAudioCategory> category = this->getFieldValue(fieldCategory);
      return SoundOptions(
        volume != nullptr ? std::make_optional(volume->value()) : std::nullopt,
        loop != nullptr ? std::make_optional(static_cast<bool>(loop->value())) : std::nullopt,
        speed != nullptr ? std::make_optional(speed->value()) : std::nullopt,
        category != nullptr ? std::make_optional(category->toCpp()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JSoundOptions::javaobject> fromCpp(const SoundOptions& value) {
      using JSignature = JSoundOptions(jni::alias_ref<jni::JDouble>, jni::alias_ref<jni::JBoolean>, jni::alias_ref<jni::JDouble>, jni::alias_ref<JAudioCategory>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        value.volume.has_value() ? jni::JDouble::valueOf(value.volume.value()) : nullptr,
        value.loop.has_value() ? jni::JBoolean::valueOf(value.loop.value()) : nullptr,
        value.speed.has_value() ? jni::JDouble::valueOf(value.speed.value()) : nullptr,
        value.category.has_value() ? JAudioCategory::fromCpp(value.category.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::nitrosonic
