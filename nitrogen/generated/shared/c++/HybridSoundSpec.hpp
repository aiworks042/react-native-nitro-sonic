///
/// HybridSoundSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `SoundOptions` to properly resolve imports.
namespace margelo::nitro::nitrosonic { struct SoundOptions; }
// Forward declaration of `NotificationOptions` to properly resolve imports.
namespace margelo::nitro::nitrosonic { struct NotificationOptions; }
// Forward declaration of `AudioCategory` to properly resolve imports.
namespace margelo::nitro::nitrosonic { enum class AudioCategory; }
// Forward declaration of `HybridSoundPlayerSpec` to properly resolve imports.
namespace margelo::nitro::nitrosonic { class HybridSoundPlayerSpec; }

#include <NitroModules/Promise.hpp>
#include <string>
#include "SoundOptions.hpp"
#include <optional>
#include "NotificationOptions.hpp"
#include "AudioCategory.hpp"
#include <memory>
#include "HybridSoundPlayerSpec.hpp"

namespace margelo::nitro::nitrosonic {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Sound`
   * Inherit this class to create instances of `HybridSoundSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSound: public HybridSoundSpec {
   * public:
   *   HybridSound(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSoundSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSoundSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSoundSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<bool>> play(const std::string& source, const std::optional<SoundOptions>& options) = 0;
      virtual std::shared_ptr<Promise<bool>> playNotification(const std::string& soundName, const std::optional<NotificationOptions>& options) = 0;
      virtual std::shared_ptr<Promise<bool>> preload(const std::string& source) = 0;
      virtual void unload(const std::string& source) = 0;
      virtual void stopAll() = 0;
      virtual void setCategory(AudioCategory category) = 0;
      virtual void setMasterVolume(double volume) = 0;
      virtual double getMasterVolume() = 0;
      virtual void setMuted(bool muted) = 0;
      virtual bool isMuted() = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<HybridSoundPlayerSpec>>> createPlayer(const std::string& source, const std::optional<SoundOptions>& options) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Sound";
  };

} // namespace margelo::nitro::nitrosonic
